version: '3.8'

services:
  postgres:
    container_name: fintech_db
    image: postgres
    ports:
      - "5435:5432"

    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=fintech_db